1.字典
	a.应用场景
		1>接口开发
		2>前端数据传输
	b.创建字典
		字典是python中的映射数据类型
		大括号中键值对组成{'name':'gaga'}
		一般是字符串或者数字作为键
	c.访问字典
		直接访问
		数据的筛选
	d.删除字典
		del
			删除指定元素(键值对)
			删除整个字典(不能访问)
		clear
			清空，可以访问
	e.修改字典
		通过筛选字典的键进行赋值修改
	f.补充内容
		1.keys(键)
		2.values(值)
		3.items(简直对)
2.文件操作
	a.文件操作应用场景
		1>网站记录日志时候
		2>报表数据
		3>爬虫爬取的数据写入到文件中
		4>招聘网站的简历的导入导出
	b.文件操作
		1>读文件
			open()函数，常见2个参数，一个文件路径，一个是文件操作
		2>写文件
			open()函数，第二个参数是w，代表写入，write只能写入字符串
			注意:如果文件被占用，是无法进行其他操作的
		3>追加文件
			open()函数的第三个参数是a

练习2：
	之前字典程序，记录登录时候输入的错误的用户名，写入到日志文件(log.txt)中
	之前字典程序，记录登录时候输入的错误的密码，写入到日志文件(log.txt)中
	文件内容如下:
	错误用户名:xxx
	错误密码:xxxx
	。。。
3.函数定义和调用
	a.什么是函数
		做开发时候使用指定的代码多次，为了提高代码效率和重用性，所以把多个功能代码组织为一个模块，就是函数
		例子1:
			1.打开网页
			2.点击用户名输入框
			3.清空用户名输入框
			4.输入用户名信息
			5.点击密码输入框
			6.清空密码输入框
			7.输入密码信息
			8.点击登录
		例子2:
			1.打开网页
			2.操作用户名
				点击xxx
				清空xxx
				输入xxx
			3.操作密码
				点击xxx
				清空xxx
				输入xxx
			4.登录
		例子3:
			定义函数
			输入信息(信息类型,信息)
				点击 信息类型 输入框
				清空 信息类型 输入框
				填写 信息
			注意:
				信息类型
					用户名
					密码
			1.打开网页
			2.调用函数 输入信息(用户名,'admin')
			3.调用函数 输入信息(密码,'123456')
			4.点击登录
	b.python中函数的定义和调用
		b1.函数定义的语法:
			def 函数名():
				函数的主体
		b2.函数的调用语法:
			函数名()
	c.函数的常用格式
		c1.无参数，无返回值(自己工作，散养)
		c2.无参数，有返回值(主动发周报)
		c3.有参数，无返回值(领导安排任务，不管效果)
		c4.有参数，有返回值(领导安排任务，汇报完成情况)
		



